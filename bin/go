#!/bin/bash

docker run                              \
    --rm                                \
    --volume "${PWD}:/code"             \
    --workdir /code                     \
    --user "$(id --user):$(id --group)" \
    golang:alpine go "${@}"
